{
  "name": "jeskola",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test:preset-integrity": "node test_preset_integrity.js",
    "test:composed-load": "node test_composed_preset_load.js",
    "test:distortion-preset": "node test_distortion_preset.js",
    "test:rapid-switch": "node test_rapid_import_switch.js",
    "test:duplicate-import": "node test_duplicate_id_import.js",
    "test:regression": "bash -lc '(npm run -s dev >/tmp/jeskola-dev.log 2>&1 &) ; DEV_PID=$!; trap \"kill $DEV_PID 2>/dev/null || true\" EXIT; sleep 2; npm run -s test:preset-integrity && node test_distortion_preset.js && node test_composed_preset_load.js && node test_rapid_import_switch.js && node test_duplicate_id_import.js'"
  },
  "devDependencies": {
    "puppeteer": "^24.37.5",
    "typescript": "~5.9.3",
    "vite": "^7.3.1"
  }
}
